const template = require("./index.html")
import './index.scss'
let WebHeader = {
    data() {
        return {
            count: 0
        }
    },
    template: template
}
export default WebHeader;
